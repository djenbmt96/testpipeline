version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install -g @angular/cli@6.0.8
  build:
    commands:
      - npm install
      - ng build
  post_build:
    commands:
      - aws s3 sync dist/ s3://diennt-pipeline --delete
